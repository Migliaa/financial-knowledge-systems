# Ambienti e cache locali
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.cache/

# Consegna sito autonoma: non fa parte della repository M1/M2/M3
mockup-portfolio/
backups/

# Credenziali
.env
.env.*
!.env.example

# Materiali voluminosi: conservare provenienza e riproduzione separatamente
data/raw/
data/processed/
artifacts/
models/

# Le run NON sono ignorate globalmente: selezionare per la pubblicazione
# risultati piccoli e riproducibili; non committare pesi o dati con vincoli.
